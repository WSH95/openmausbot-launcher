# Open questions

Record questions that the repository does not answer instead of guessing.
Add the answer to the item when it is resolved.

- [ ] Does an OpenClaw automation with `--announce` deliver an empty message
      when the command prints nothing? (Needs an OpenClaw install.)
- [ ] Does DeepSeek Harness load `~/.agents/skills` in the release the user
      runs? (Third-party posts say tier 500; the README is silent.)
